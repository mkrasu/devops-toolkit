apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-dashboard.fullname" . }}
  labels:
    {{- include "devops-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
  selector:
    {{- include "devops-dashboard.selectorLabels" . | nindent 4 }}
